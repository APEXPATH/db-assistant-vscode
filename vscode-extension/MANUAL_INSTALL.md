# Manual VS Code Extension Installation

## ✅ Extension Built Successfully!
File: `database-ai-assistant-0.0.1.vsix` (8KB)

## 🔧 Install Methods:

### Method 1: VS Code UI (Easiest)
1. **Open VS Code**
2. **Go to Extensions** (`Cmd+Shift+X`)
3. **Click "..." menu** (top right of Extensions panel)
4. **Select "Install from VSIX..."**
5. **Navigate to:** `/Users/munnabhakta/Research/AI-Agents/First/vscode-extension/`
6. **Select:** `database-ai-assistant-0.0.1.vsix`
7. **Click Install**

### Method 2: Drag & Drop
1. **Open VS Code**
2. **Drag** `database-ai-assistant-0.0.1.vsix` **into VS Code window**
3. **Click Install** when prompted

### Method 3: Command Line (if code command works)
```bash
# First enable code command in VS Code:
# Cmd+Shift+P → "Shell Command: Install 'code' command in PATH"
# Then:
code --install-extension database-ai-assistant-0.0.1.vsix
```

## 🚀 After Installation:
1. **Restart VS Code**
2. **Press `Cmd+Shift+P`**
3. **Type:** "Set Database Connection"
4. **Enter your PostgreSQL connection string**
5. **Enter your Gemini API key**
6. **Press `Cmd+Shift+Q` to ask questions!**

## 📍 Extension Location:
`/Users/munnabhakta/Research/AI-Agents/First/vscode-extension/database-ai-assistant-0.0.1.vsix`